"""ข้อความ prompt ทั้งหมดสำหรับการสร้างข้อสอบ

แยกออกมาจาก quiz_service.py เพื่อให้แก้ข้อความได้โดยไม่ต้องแตะตรรกะ
และเพื่อให้เปรียบเทียบกติกาของสองโหมดได้ในไฟล์เดียว

โหมดที่รองรับ
  source  = ถามจากเนื้อหา (ของเดิม ค่าเริ่มต้น) ข้อความทุกตัวอักษรเหมือนเดิม
  applied = ประยุกต์ แต่งโจทย์ใหม่จากกฎในเอกสาร
"""

MODE_SOURCE = "source"
MODE_APPLIED = "applied"
VALID_MODES = (MODE_SOURCE, MODE_APPLIED)


def normalize_mode(mode) -> str:
    """กันค่าแปลกปลอม ถ้าไม่รู้จักให้ถอยกลับไปโหมดเดิมเสมอ"""
    key = (mode or MODE_SOURCE).strip().lower()
    return key if key in VALID_MODES else MODE_SOURCE


# ---------------------------------------------------------------------------
# ระดับความยาก
# ---------------------------------------------------------------------------

DIFFICULTY_PROMPTS = {
    # ----- โหมดเดิม: คัดลอกมาจาก quiz_service.py เดิมทุกตัวอักษร ห้ามแก้ -----
    MODE_SOURCE: {
        "easy": """ระดับความยาก: ง่าย
- ถามข้อเท็จจริงตรง ๆ ที่ปรากฏชัดเจนในเนื้อหา จำได้ก็ตอบได้
- ห้ามต้องตีความหรือวิเคราะห์
- ตัวเลือกลวงต้องผิดชัดเจน แยกออกง่ายทันที""",
        "medium": """ระดับความยาก: ปานกลาง
- ต้องเข้าใจความหมาย ไม่ใช่แค่จำข้อความ
- อาจต้องเชื่อมโยงข้อมูล 2 จุดในเนื้อหาเข้าด้วยกัน
- ตัวเลือกลวงดูมีเหตุผลระดับหนึ่ง ต้องอ่านให้ดีก่อนตัด""",
        "hard": """ระดับความยาก: ยาก
- ต้องใช้การวิเคราะห์ เปรียบเทียบ คำนวณ หรือเชื่อมโยงข้อมูลหลายจุดเข้าด้วยกัน จึงจะตอบได้
- คำตอบต้องไม่ใช่ข้อความที่ลอกมาจากเนื้อหาตรง ๆ ผู้ตอบต้องคิดต่อเอง
- ห้ามถามวนกลับ: ห้ามให้โจทย์บอกคำตอบไว้ในตัวคำถามเอง
- ห้ามใส่ฉากสมมติที่ไม่ได้เพิ่มการคิด (ห่อคำถามง่ายด้วยสถานการณ์ปลอม)
- ตัวเลือกลวงต้องใกล้เคียงคำตอบจริง ถูกบางส่วนแต่ผิดรายละเอียด จนต้องคิดรอบคอบ
- ถ้าเนื้อหาไม่ลึกพอจะออกข้อยากได้จริง ให้ออกข้อที่ต้องคำนวณหรือเปรียบเทียบจากข้อมูลที่มี แทนการแต่งฉากสมมติ""",
    },

    # ----- โหมดประยุกต์: เขียนใหม่ ใช้คำกลาง ๆ ให้ครอบคลุมทุกวิชา -----
    MODE_APPLIED: {
        "easy": """ระดับความยาก: ง่าย
- ใช้กฎเดียวจากเนื้อหา ตัดสินได้จากข้อมูลในโจทย์โดยตรง
- เปลี่ยนเฉพาะข้อมูลในโจทย์ให้เป็นชุดใหม่
  (ตัวเลขชุดใหม่ ประโยคใหม่ หรือกรณีใหม่ แล้วแต่ลักษณะของเนื้อหา)
- ห้ามถามย้อนจากผลลัพธ์กลับไปหาสิ่งที่ทำให้เกิดผลนั้น
- ตัวเลือกลวงต้องผิดชัดเจน แยกออกง่ายทันที""",
        "medium": """ระดับความยาก: ปานกลาง
- ใช้กฎเดียวจากเนื้อหา แต่ข้อมูลในโจทย์ไม่ได้ชี้คำตอบโดยตรง
  ผู้ตอบต้องตีความหรือแปลงข้อมูลก่อนหนึ่งชั้น
- หรือถามย้อนจากผลลัพธ์กลับไปหาสิ่งที่ทำให้เกิดผลนั้น
- โครงสร้างโจทย์ต้องต่างจากตัวอย่างในเนื้อหาอย่างเห็นได้ชัด
- ตัวเลือกลวงควรเป็นสิ่งที่ได้จากความเข้าใจผิดที่พบบ่อย""",
        "hard": """ระดับความยาก: ยาก
- ต้องใช้กฎตั้งแต่สองข้อขึ้นไปจากเนื้อหาร่วมกันจึงจะตอบได้
- ผู้ตอบต้องเลือกเองว่าจะใช้กฎข้อใด โจทย์ต้องไม่บอกใบ้
- ห้ามเพิ่มความยากด้วยกฎที่เนื้อหาไม่ได้สอน
  ให้เพิ่มความยากด้วยจำนวนกฎที่ต้องใช้ หรือความซับซ้อนของโจทย์แทน
- ตัวเลือกลวงต้องเป็นสิ่งที่ได้จากการใช้กฎถูกแต่ผิดจุด หรือใช้กฎไม่ครบ""",
    },
}


def difficulty_block(difficulty, mode=MODE_SOURCE) -> str:
    """คืนบล็อกระดับความยากตามโหมด ถ้าค่าไม่ถูกต้องให้ถอยไป medium เหมือนเดิม"""
    table = DIFFICULTY_PROMPTS[normalize_mode(mode)]
    key = (difficulty or "medium").strip().lower()
    return table.get(key, table["medium"]) + "\n"


# ---------------------------------------------------------------------------
# กติกาความถูกต้อง (ปรนัย)
# ---------------------------------------------------------------------------

# โหมดเดิม: คัดลอกมาจาก prompt เดิมทุกตัวอักษร ห้ามแก้
_ANSWER_RULES_MCQ_SOURCE = """*** ความถูกต้องของคำตอบ (สำคัญที่สุด) ***
- คำตอบที่ถูกต้องจริง "ต้องปรากฏอยู่ในรายการตัวเลือก" เสมอ
  ห้ามตั้งคำถามที่คำตอบจริงไม่ได้อยู่ในตัวเลือกใดเลย — ผู้สอบต้องตอบได้
- ก่อนสรุปแต่ละข้อ ให้ตรวจว่า "ตัวเลือกที่ระบุใน answer คือคำตอบที่ถูกต้องจริง" ตามเนื้อหา
- คำตอบต้องตรวจสอบย้อนกลับได้จากเนื้อหาที่ให้มาเท่านั้น ห้ามใช้ความรู้นอกเนื้อหา
- ถ้าเนื้อหาไม่ได้ระบุคำตอบไว้ชัดเจน ให้เปลี่ยนไปถามประเด็นอื่นที่เนื้อหาระบุไว้ชัด"""

# กติกากลางของโหมดประยุกต์ ใช้ร่วมกันทั้งปรนัยและถูก/ผิด
_APPLIED_CORE = """*** โหมดประยุกต์ (สำคัญที่สุด) ***
- ห้ามใช้ตัวเลข ประโยค หรือตัวอย่างที่ปรากฏในเนื้อหามาเป็นโจทย์โดยตรง
  ให้แต่งโจทย์ใหม่ที่ไม่มีในเนื้อหา
- แต่ต้องใช้ "กฎ สูตร หรือหลักการ" ที่เนื้อหาสอนไว้เท่านั้น
  ห้ามใช้กฎหรือความรู้ที่เนื้อหาไม่ได้สอน แม้จะเป็นความรู้ที่ถูกต้องก็ตาม
- ถ้าเนื้อหาไม่ได้เขียนกฎไว้ตรง ๆ แต่มีตัวอย่างหลายตัว
  ให้สรุปกฎจากตัวอย่างเหล่านั้นก่อน แล้วจึงใช้กฎนั้นแต่งโจทย์ใหม่
- เกณฑ์ตัดสิน: ผู้ที่อ่านเนื้อหานี้เข้าใจครบถ้วน ต้องตอบได้ทุกข้อ
  ถ้าต้องรู้อะไรเพิ่มจากภายนอกจึงจะตอบได้ ข้อนั้นใช้ไม่ได้ ให้เปลี่ยนใหม่
- ห้ามเปลี่ยนเรื่อง เนื้อหาสอนเรื่องใด โจทย์ต้องอยู่ในเรื่องนั้น
- ถ้าเนื้อหามีกฎหลายข้อ ให้กระจายโจทย์ไปทุกกฎ ห้ามกระจุกอยู่กฎเดียว

*** ประเด็นที่แต่งโจทย์ใหม่ไม่ได้ ***
บางประเด็นไม่มีกฎให้นำไปใช้กับข้อมูลชุดใหม่ ได้แก่ นิยาม ความหมายของศัพท์
สัญลักษณ์ ปี พ.ศ./ค.ศ. ชื่อบุคคล ชื่อสถานที่ เลขมาตรา
และข้อเท็จจริงเฉพาะตัวที่เปลี่ยนแปลงไม่ได้
- ประเด็นเหล่านี้ให้ถามจากเนื้อหาตามปกติ
- การแต่งปี ตัวเลข หรือชื่อเฉพาะที่ไม่มีในเนื้อหา ถือเป็นความผิดพลาดร้ายแรง

*** ความถูกต้อง ***
- คำตอบไม่ได้อยู่ในเนื้อหา ผู้ตรวจจึงเทียบย้อนกลับไม่ได้
  ความถูกต้องขึ้นอยู่กับการตรวจทานของคุณเพียงอย่างเดียว
- ก่อนสรุปแต่ละข้อ ให้ตรวจทานคำตอบใหม่อีกครั้ง
  ถ้าเป็นการคำนวณให้คำนวณซ้ำ ถ้าเป็นการใช้กฎให้ไล่กฎซ้ำทีละข้อ
- ข้อมูลที่แต่งขึ้นต้องสมเหตุสมผลตามธรรมชาติของเรื่องนั้น
- ถ้าไม่มั่นใจในคำตอบ ให้เปลี่ยนไปใช้โจทย์ที่ง่ายกว่าและมั่นใจได้
  ความถูกต้องสำคัญกว่าความยาก

*** การเขียนโจทย์ ***
- ห้ามใส่คำว่า "ตามเนื้อหา" "ในเอกสาร" หรือคำทำนองนี้ในตัวคำถาม
  ข้อสอบต้องอ่านเข้าใจได้โดยไม่ต้องถือเอกสาร
- ห้ามให้โจทย์บอกคำตอบไว้ในตัวคำถามเอง
- ห้ามใส่ข้อมูลที่ไม่จำเป็นต้องใช้ในการหาคำตอบ
- ช่อง explain ต้องแสดงวิธีคิดจนได้คำตอบ
  ห้ามเขียนเพียงว่าเนื้อหาระบุไว้ที่ใด"""

_ANSWER_RULES_MCQ_APPLIED = """*** ความถูกต้องของคำตอบ (สำคัญที่สุด) ***
- คำตอบที่ถูกต้องจริง "ต้องปรากฏอยู่ในรายการตัวเลือก" เสมอ
  ห้ามตั้งคำถามที่คำตอบจริงไม่ได้อยู่ในตัวเลือกใดเลย — ผู้สอบต้องตอบได้
- ก่อนสรุปแต่ละข้อ ให้ตรวจว่า "ตัวเลือกที่ระบุใน answer คือคำตอบที่ถูกต้องจริง"

""" + _APPLIED_CORE

ANSWER_RULES_MCQ = {
    MODE_SOURCE: _ANSWER_RULES_MCQ_SOURCE,
    MODE_APPLIED: _ANSWER_RULES_MCQ_APPLIED,
}

# ---------------------------------------------------------------------------
# กติกาความถูกต้อง (ถูก/ผิด)
# โหมดเดิมไม่มีบล็อกนี้ใน prompt จึงต้องเป็นค่าว่าง เพื่อให้ข้อความเหมือนเดิมเป๊ะ
# ---------------------------------------------------------------------------

_ANSWER_RULES_TF_APPLIED = _APPLIED_CORE + """

*** การกระจายเฉลย ***
- กระจายเฉลยให้ "จริง" กับ "เท็จ" มีจำนวนใกล้เคียงกัน
- ข้อที่เฉลยเป็นเท็จ ให้ใช้ข้อมูลที่ผิดแบบที่ผู้เรียนมักเข้าใจผิดจริง
  ไม่ใช่ข้อมูลที่ผิดแบบสุ่ม"""

ANSWER_RULES_TF = {
    MODE_SOURCE: "",
    MODE_APPLIED: _ANSWER_RULES_TF_APPLIED,
}


# ---------------------------------------------------------------------------
# รายการห้ามซ้ำ
# ---------------------------------------------------------------------------

def exclude_block(exclude_list, limit, mode=MODE_SOURCE) -> str:
    """สร้างบล็อกรายการข้อที่มีอยู่แล้ว

    exclude_list รับได้ 2 แบบ
      - str            : คำถามอย่างเดียว (แบบเดิม)
      - {"question", "answer"} : คำถามพร้อมเฉลย (แบบใหม่)
    """
    items = list(exclude_list or [])[:limit]
    if not items:
        return ""

    has_answer = any(isinstance(x, dict) and x.get("answer") for x in items)

    # ไม่มีเฉลยส่งมา -> ใช้ข้อความเดิมทุกตัวอักษร
    if not has_answer:
        lines = [str(x.get("question") if isinstance(x, dict) else x) for x in items]
        return "หลีกเลี่ยงการตั้งคำถามคล้ายกับ:\n" + "\n".join(f"- {q}" for q in lines) + "\n"

    lines = []
    for x in items:
        if isinstance(x, dict):
            q = str(x.get("question") or "").strip()
            a = str(x.get("answer") or "").strip()
        else:
            q, a = str(x).strip(), ""
        if not q:
            continue
        lines.append(f"- ถาม: {q}" + (f"\n  ตอบ: {a}" if a else ""))

    if not lines:
        return ""

    tail = (
        "ถือว่าซ้ำเมื่อวัดกฎเดียวกันและใช้ข้อมูลชุดเดียวกัน\n"
        "ถ้าใช้กฎเดียวกันแต่เปลี่ยนข้อมูลในโจทย์เป็นชุดใหม่ ไม่ถือว่าซ้ำ\n"
        if normalize_mode(mode) == MODE_APPLIED else ""
    )
    return "ข้อสอบที่มีอยู่แล้ว ห้ามออกข้อที่ซ้ำกับรายการนี้:\n" + "\n".join(lines) + "\n" + tail


# ---------------------------------------------------------------------------
# เกณฑ์ตัดข้อซ้ำ
# ---------------------------------------------------------------------------

# โหมดประยุกต์ตั้งใจให้โจทย์หน้าตาคล้ายกันแต่เปลี่ยนค่า จึงต้องใช้เกณฑ์สูงกว่ามาก
# มิฉะนั้นโจทย์ที่ถูกต้องจะถูกตัดทิ้ง (วัดจริงได้ 0.93-0.96)
APPLIED_NEAR_DUP_THRESHOLD = 0.97


def near_dup_threshold(mode, default: float) -> float:
    return APPLIED_NEAR_DUP_THRESHOLD if normalize_mode(mode) == MODE_APPLIED else default